import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  fullScreen: {
    flex: 1,
  },
  mt50: {
    marginTop: 50
  },
  minHeight: {
    minHeight: 300
  },
  mt100: {
    marginTop: 100
  }
});

export default commonStyles;
